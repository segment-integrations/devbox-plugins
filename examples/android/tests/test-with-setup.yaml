version: "0.5"

log_level: debug

processes:
  test-setup:
    command: |
      echo '[TEST] Starting...'
      echo '[TEST] About to source setup.sh...'
      . ${ANDROID_FLAKE_DIR}/scripts/init/setup.sh
      echo '[TEST] setup.sh sourced successfully'
      echo '[TEST] ANDROID_SDK_ROOT='${ANDROID_SDK_ROOT}
      echo '[TEST] Done!'
    availability:
      restart: "no"
