version: "0.5"

log_location: "reports/android-build-logs"
log_level: info

processes:
  build-app:
    command: "echo '📦 Building Android app...' && gradle assembleDebug --info && echo '✓ Build complete'"
    availability:
      restart: "no"
