name: PR Fast Checks

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  shellcheck:
    name: Shellcheck Validation
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Install Devbox
        uses: jetify-com/devbox-install-action@v0.14.0
        with:
          enable-cache: true

      - name: Run shellcheck on all plugins
        run: devbox run lint-all

  android-unit-tests:
    name: Android Plugin Unit Tests
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Install Devbox
        uses: jetify-com/devbox-install-action@v0.14.0
        with:
          enable-cache: true

      - name: Run Android unit tests
        run: devbox run test-android

  android-plugin-tests:
    name: Android Plugin Tests
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Setup Gradle cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Install Devbox
        uses: jetify-com/devbox-install-action@v0.14.0
        with:
          enable-cache: true

      - name: Run Android validation tests
        run: bash devbox/plugins/tests/android/test-validation.sh

      - name: Run Android device management tests
        run: bash devbox/plugins/tests/android/test-device-mgmt.sh

  ios-plugin-tests:
    name: iOS Plugin Tests
    runs-on: macos-15
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Setup CocoaPods cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cocoapods/repos
            ~/Library/Caches/CocoaPods
          key: ${{ runner.os }}-pods-${{ hashFiles('**/Podfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-pods-

      - name: Setup iOS build cache
        uses: actions/cache@v4
        with:
          path: |
            ~/Library/Developer/Xcode/DerivedData
          key: ${{ runner.os }}-xcode-${{ hashFiles('**/*.xcodeproj/**', '**/*.xcworkspace/**') }}
          restore-keys: |
            ${{ runner.os }}-xcode-

      - name: Install Devbox
        uses: jetify-com/devbox-install-action@v0.14.0
        with:
          enable-cache: true

      - name: Run iOS device management tests
        run: bash devbox/plugins/tests/ios/test-device-mgmt.sh

      - name: Run iOS cache tests
        run: bash devbox/plugins/tests/ios/test-cache.sh

  android-quick-smoke:
    name: Android Quick Smoke Test
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Setup Gradle cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Select device for SDK evaluation
        working-directory: devbox/examples/android
        run: |
          # Select only 'min' device to minimize SDK download in CI
          # (devices.sh uses nix-shell for jq if not installed)
          bash ../../plugins/android/scripts/devices.sh select min

      - name: Install Devbox
        uses: jetify-com/devbox-install-action@v0.14.0
        with:
          enable-cache: true

      - name: Test Android emulator lifecycle (min device)
        working-directory: devbox/examples/android
        env:
          EMU_HEADLESS: 1
        run: |
          # Start emulator and wait for boot
          devbox run --pure start-emu min

          # Verify emulator is running
          devbox run --pure android.sh info

          # Clean stop
          devbox run --pure stop-emu

  ios-quick-smoke:
    name: iOS Quick Smoke Test
    runs-on: macos-15
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Setup CocoaPods cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cocoapods/repos
            ~/Library/Caches/CocoaPods
          key: ${{ runner.os }}-pods-${{ hashFiles('**/Podfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-pods-

      - name: Setup iOS build cache
        uses: actions/cache@v4
        with:
          path: |
            ~/Library/Developer/Xcode/DerivedData
          key: ${{ runner.os }}-xcode-${{ hashFiles('**/*.xcodeproj/**', '**/*.xcworkspace/**') }}
          restore-keys: |
            ${{ runner.os }}-xcode-

      - name: Install Devbox
        uses: jetify-com/devbox-install-action@v0.14.0
        with:
          enable-cache: true

      - name: Test iOS simulator lifecycle (default device)
        working-directory: devbox/examples/ios
        env:
          SIM_HEADLESS: 1
        run: |
          # Start simulator and wait for boot
          devbox run --pure start-sim

          # Verify simulator is running
          devbox run --pure ios.sh info

          # Clean stop
          devbox run --pure stop-sim

  android-unit-tests:
    name: Android Unit Tests
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Setup Gradle cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Install Devbox
        uses: jetify-com/devbox-install-action@v0.14.0
        with:
          enable-cache: true

      - name: Run Android unit tests
        working-directory: devbox/examples/android
        run: devbox run --pure test-android

  ios-unit-tests:
    name: iOS Unit Tests
    runs-on: macos-15
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Setup iOS build cache
        uses: actions/cache@v4
        with:
          path: |
            ~/Library/Developer/Xcode/DerivedData
          key: ${{ runner.os }}-xcode-${{ hashFiles('**/*.xcodeproj/**', '**/*.xcworkspace/**') }}
          restore-keys: |
            ${{ runner.os }}-xcode-

      - name: Install Devbox
        uses: jetify-com/devbox-install-action@v0.14.0
        with:
          enable-cache: true

      - name: Run iOS unit tests
        working-directory: devbox/examples/ios
        run: devbox run --pure test-ios

  react-native-unit-tests:
    name: React Native Unit Tests
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js with cache
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: devbox/examples/react-native/package-lock.json

      - name: Install Devbox
        uses: jetify-com/devbox-install-action@v0.14.0
        with:
          enable-cache: true

      - name: Install dependencies
        working-directory: devbox/examples/react-native
        run: devbox run --pure build-node

      - name: Run Jest tests
        working-directory: devbox/examples/react-native
        run: npm test -- --coverage

  shellcheck:
    name: Shellcheck Validation
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Install Devbox
        uses: jetify-com/devbox-install-action@v0.14.0
        with:
          enable-cache: true

      - name: Run shellcheck on all plugins
        run: devbox run lint-all

  android-plugin-unit-tests:
    name: Android Plugin Unit Tests
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Install Devbox
        uses: jetify-com/devbox-install-action@v0.14.0
        with:
          enable-cache: true

      - name: Run Android plugin unit tests
        run: devbox run test-android

  status-check:
    name: All PR Checks Passed
    runs-on: ubuntu-latest
    needs: [shellcheck, android-plugin-unit-tests, android-plugin-tests, ios-plugin-tests, android-quick-smoke, ios-quick-smoke, android-unit-tests, ios-unit-tests, react-native-unit-tests]
    if: always()
    steps:
      - name: Check job results
        run: |
          if [[ "${{ needs.shellcheck.result }}" != "success" ]] || \
             [[ "${{ needs.android-plugin-unit-tests.result }}" != "success" ]] || \
             [[ "${{ needs.android-plugin-tests.result }}" != "success" ]] || \
             [[ "${{ needs.ios-plugin-tests.result }}" != "success" ]] || \
             [[ "${{ needs.android-quick-smoke.result }}" != "success" ]] || \
             [[ "${{ needs.ios-quick-smoke.result }}" != "success" ]] || \
             [[ "${{ needs.android-unit-tests.result }}" != "success" ]] || \
             [[ "${{ needs.ios-unit-tests.result }}" != "success" ]] || \
             [[ "${{ needs.react-native-unit-tests.result }}" != "success" ]]; then
            echo "One or more checks failed"
            exit 1
          fi
          echo "All checks passed!"
